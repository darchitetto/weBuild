import React from 'react';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        alignSelf: 'center',
        width:'80%',
        height: '90%',
        paddingTop: 50
    },
    preview: {
        width: '100%',
        height: '100%'
    },
});
